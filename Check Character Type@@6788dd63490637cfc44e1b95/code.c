#include <stdio.h>
int main() {
    char ch;
    scanf("%c",&ch);
    if( a == 'a' || a == 'e' || a== 'i' || a== 'o' || a== 'u')
    { 
        printf("Vowel");
    }
    else
    {
        printf("Consonant");
    }
}